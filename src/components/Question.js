import React from 'react';
import Answers from './Answers';
import Player from './Player';

const Question = () => {
  return (
    <div>
      <Player />
      <Answers />
    </div>
  );
};

export default Question;
